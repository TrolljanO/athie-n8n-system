# ==============================================
# N8N-system - ATHIÉ WOHNRATH | TRAJANO
# Modelo de Configuraçaão de ambiente
# ==============================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Preencha os valores reais
# 3. NUNCA commite o arquivo .env no Git
#

# --- Project ---
COMPOSE_PROJECT_NAME=athie

# --- N8N Configuration ---
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/
N8N_TIMEZONE=America/Sao_Paulo
N8N_LOG_LEVEL=info

# --- PostgreSQL Database ---
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=athiedocs
POSTGRES_USER=n8nathie
POSTGRES_PASSWORD=sua-senha-segura-aqui
POSTGRES_SCHEMA=n8n_athie_schema

# --- Redis Cache ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=sua-senha-redis-aqui

# --- API Keys ---
# Webhook principal (autenticação X-API-Key)
WEBHOOK_API_KEY=sua-api-key-principal-aqui

# Webhook SST (decisão manual)
WEBHOOK_SST_API_KEY=sua-api-key-sst-aqui

# Google Cloud Vision (OCR - opcional se usar OpenRouter)
GOOGLE_CLOUD_VISION_API_KEY=sua-google-vision-key-ou-deixe-vazio

# OpenRouter (Gemini 2.0 Flash Thinking)
OPENROUTER_API_KEY=sk-or-v1-sua-chave-aqui

# --- Google Drive ---
# IDs das pastas (obter da URL: drive.google.com/drive/folders/ID_AQUI)
GOOGLE_DRIVE_FOLDER_RECEBIDOS=id-pasta-1-recebidos
GOOGLE_DRIVE_FOLDER_PROCESSANDO=id-pasta-2-processando
GOOGLE_DRIVE_FOLDER_VALIDADOS=id-pasta-3-validados
GOOGLE_DRIVE_FOLDER_REVISAO=id-pasta-4-revisao-manual
GOOGLE_DRIVE_FOLDER_RECUSADOS=id-pasta-5-recusados
GOOGLE_DRIVE_FOLDER_NAO_CLASSIFICADOS=id-pasta-nao-classificados

# Service Account JSON (credenciais do Google Drive API)
# Gerar em: https://console.cloud.google.com/iam-admin/serviceaccounts
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"seu-projeto",...}

# --- Gmail SMTP (Notificações) ---
GMAIL_USER=seu-email@gmail.com
GMAIL_APP_PASSWORD=sua-senha-app-16-digitos
GMAIL_FROM_NAME=N8N Athie System

# Email da equipe SST (para notificações de revisão manual)
EMAIL_SST=sst@athie.com.br

# --- Configurações Opcionais ---
# Habilitar debug detalhado
DEBUG=false

# Limite de tamanho de arquivo (em MB)
MAX_FILE_SIZE_MB=10

# Timeout para processamento (em segundos)
PROCESSING_TIMEOUT=300
